# Examples

This directory contains small derived artifacts for demonstration:

- `regional_precipitation_summary_1961_2022.csv`: regional July precipitation summary;
- `sample_metadata.json`: lightweight metadata describing the example scope.

These files are not raw climate datasets.
